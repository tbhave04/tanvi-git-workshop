import random
import time
import os
import sys



# About 30 functions to implement

# Returns 1 with probability p for heads, 0 for tails otherwise. If p is not in [0.1,0.9], raises a ValueError.
def flip_coin(p):
    # TODO
    pass

# Returns the average value of flipping a coin with probability of heads p, n times, using [flip_coin]. You can assume n > 0.
def average_of_flips(n, p):
    # TODO
    pass

# Returns the median value of flipping a coin with probability of heads p, n times using [flip_coin]. You can assume n > 0.
def median_of_flips(n, p):
    # TODO
    pass


# If lst and filter are both lists, returns a new list with all elements of lst that are not in filter.
def remove_all(lst, filter):
    # TODO
    pass
    
# Return string s with all vowels removed. If s is not a string, raise a TypeError.
def remove_vowels(s):
    # TODO
    pass